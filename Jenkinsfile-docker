pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "git clone https://github.com/salvadiaz/spring-boot-IS3.git"
                sh "cd spring-boot-IS3"
                sh "docker build -t salvadiazpesci/spring-boot ."
            }
        }
        stage('Push image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker-credential', passwordVariable: 'PASS', usernameVariable: 'USER')]) {
                    sh "docker login -u salvadiazpesci -p 40750160sdsd"
                    sh "docker push salvadiazpesci/spring-boot:${env.BUILD_ID}"
                }
            }
        }
    }
}
